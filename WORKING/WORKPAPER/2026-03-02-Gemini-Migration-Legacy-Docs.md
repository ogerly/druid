# Workpaper: Migration of Legacy `docs/` Folder

**_Session Start:_** 2026-03-02

## 1. Session Goal

To execute the migration plan outlined in `2026-03-02-Gemini-Issue-Legacy-Docs-Report.md`. This involves reviewing, migrating, and consolidating all relevant files from the legacy `docs/` directory into the official `WORKING/` AAMS workspace, and then deprecating the `docs/` folder.

## 2. Migration Plan Execution

This workpaper will serve as the live log for the migration. The file protocol will be updated as files are moved and processed.

### Step 1: List Files in `docs/`

First, I will list the contents of the `docs/` directory and its subdirectories to get a complete inventory.

### Step 2: Triage & Move

- **Conceptual Docs:** Files like `Konzept.md` and `Ideen und gedanken zu DRUID.md` will be moved to `WORKING/WHITEPAPER/`.
- **Old Workpapers:** All files from `docs/WORKPAPER/` will be moved to `WORKING/WORKPAPER/closed/`. The original filenames (with their historical dates) will be preserved.

### Step 3: Update Memory

Key insights from the migrated documents will be summarized and added to `WORKING/MEMORY/ltm-index.md`.

### Step 4: Deprecate `docs/`

The `docs/` folder will be deleted after verification.

## 3. File Protocol

*This section will be populated as actions are completed.*

| Action | From | To | Notes |
| :--- | :--- | :--- | :--- |

## 4. Next Steps

Begin with Step 1: Listing files.
